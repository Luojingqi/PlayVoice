<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:pages="clr-namespace:PlayVoice.Pages">

    <Style x:Key="TransparentComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="MaxDropDownHeight" Value="400"/>
        <Setter Property="Width" Value="120"/>

        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
                    <Setter Property="Height" Value="30"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentColorSecondary}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BgDark}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <TextBlock Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"
                                    Margin="4,1,7,1"
                                    VerticalAlignment="Center"
                                    SnapsToDevicePixels="True"
                                    Foreground="{TemplateBinding Foreground}"
                                    FontFamily="{TemplateBinding FontFamily}"/>
                        </Border>
                        <!--状态条-->
                        <Border x:Name="StatusBar"
                            Width="4"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            CornerRadius="1,2,2,1"
                            Padding="0,1,1,2"/>

                        <!-- 透明覆盖按钮（触发下拉） -->
                        <ToggleButton x:Name="DropDownToggle"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Focusable="False"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            BorderBrush="{TemplateBinding BorderBrush}"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <!-- 下拉列表 Popup -->
                        <Popup x:Name="Popup"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                               Placement="Bottom"
                               StaysOpen="False"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide" HorizontalAlignment="Stretch">
                            <Border x:Name="DropDownBorder"
                                    Background="{DynamicResource BgMedium}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    Margin="0,2,0,0"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer x:Name="DropDownScrollViewer"
                                              HorizontalScrollBarVisibility="Auto"
                                              VerticalScrollBarVisibility="Auto"
                                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ItemsPresenter x:Name="ItemsPresenter"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColorSecondary}"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColorSecondary}"/>
                        </Trigger>

                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), 
                                                  RelativeSource={RelativeSource Self}}" 
                                     Value="None">
                            <Setter TargetName="StatusBar" Property="Background" Value="Transparent"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), 
                                                  RelativeSource={RelativeSource Self}}" 
                                     Value="Error">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Error}"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), 
                                                  RelativeSource={RelativeSource Self}}" 
                                     Value="Warning">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Warning}"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), 
                                                  RelativeSource={RelativeSource Self}}" 
                                     Value="Success">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Success}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>