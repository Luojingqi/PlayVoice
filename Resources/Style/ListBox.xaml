<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:pages="clr-namespace:PlayVoice.Pages"
                    xmlns:HorizontalButtonGroup="clr-namespace:PlayVoice.Pages.HorizontalButtonGroup"
                    xmlns:TransparentAndBottomLineListBox="clr-namespace:PlayVoice.Pages.TransparentAndBottomLineListBox"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Style x:Key="TransparentListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
                    <Setter Property="Height" Value="28"/>
                    <Setter Property="Padding" Value="4,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0"
                                        CornerRadius="2"
                                        Margin="1,0">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentColorSecondary}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BgDark}"/>
                                        <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Padding="1"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                            HorizontalAlignment="Stretch"/>
                        </Border>

                        <Border x:Name="StatusBar"
                                Height="4"
                                VerticalAlignment="Bottom"
                                Background="Transparent"
                                CornerRadius="0,0,2,2"
                                Margin="1,0,1,1"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColorSecondary}"/>
                        </Trigger>

                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), RelativeSource={RelativeSource Self}}" Value="None">
                            <Setter TargetName="StatusBar" Property="Background" Value="Transparent"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), RelativeSource={RelativeSource Self}}" Value="Error">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Error}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), RelativeSource={RelativeSource Self}}" Value="Warning">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Warning}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding (pages:LabelStatusHelper.LabelStatus), RelativeSource={RelativeSource Self}}" Value="Success">
                            <Setter TargetName="StatusBar" Property="Background" Value="{DynamicResource Success}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TransparentAndBottomLineItemStyle" TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="Margin" Value="6.5,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid x:Name="RootGrid" RenderTransformOrigin="0.5,1.0">
                        <Grid.RenderTransform>
                            <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                        </Grid.RenderTransform>

                        <Border x:Name="BorderBottomLine"
                            Background="{DynamicResource BgMedium}"
                            BorderBrush="{DynamicResource AccentColor}"
                            BorderThickness="1,1,1,3"
                            CornerRadius="3,3,0,0">
                            <Grid>
                                <Border x:Name="BgBottom" Background="{DynamicResource AccentColor}" Opacity="0" CornerRadius="2,2,0,0"/>
                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Bottom"
                                              Margin="8,4,8,4"/>
                            </Grid>
                        </Border>

                        <Border x:Name="BorderTopLine"
                            Background="{DynamicResource BgMedium}"
                            BorderBrush="{DynamicResource AccentColor}"
                            BorderThickness="1,3,1,1"
                            CornerRadius="0,0,3,3"
                            Visibility="Collapsed">
                            <Grid>
                                <Border x:Name="BgTop" Background="{DynamicResource AccentColor}" Opacity="0" CornerRadius="0,0,2,2"/>
                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Top"
                                              Margin="8,4,8,4"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="BorderBottomLine" Property="BorderBrush" Value="Transparent"/>
                            <Setter TargetName="BorderTopLine" Property="BorderBrush" Value="Transparent"/>

                            <Trigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="AnimNormal"/>
                                <StopStoryboard BeginStoryboardName="AnimHover"/>
                                <BeginStoryboard x:Name="AnimSelected">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.15" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.15" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="BgBottom" Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.25"/>
                                        <DoubleAnimation Storyboard.TargetName="BgTop" Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.25"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderBottomLine" Property="BorderBrush" Value="Transparent"/>
                            <Setter TargetName="BorderTopLine" Property="BorderBrush" Value="Transparent"/>
                            <MultiTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="AnimNormal"/>
                                <StopStoryboard BeginStoryboardName="AnimSelected"/>
                                <BeginStoryboard x:Name="AnimHover">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="BgBottom" Storyboard.TargetProperty="Opacity" To="0.3" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="BgTop" Storyboard.TargetProperty="Opacity" To="0.3" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderBottomLine" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            <Setter TargetName="BorderTopLine" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            <MultiTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="AnimHover"/>
                                <StopStoryboard BeginStoryboardName="AnimSelected"/>
                                <BeginStoryboard x:Name="AnimNormal">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="BgBottom" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetName="BgTop" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>

                        <DataTrigger Binding="{Binding Path=(TransparentAndBottomLineListBox:ListBoxHelper.LinePosition), RelativeSource={RelativeSource AncestorType=ListBox}}" Value="Top">
                            <Setter TargetName="BorderBottomLine" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="BorderTopLine" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="RootGrid" Property="RenderTransformOrigin" Value="0.5,0.0"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TransparentAndBottomLineListBoxStyle" TargetType="ListBox">
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle" Value="{StaticResource TransparentAndBottomLineItemStyle}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Grid>
                        <Border x:Name="LineBottom"
                            BorderBrush="{DynamicResource AccentColor}"
                            BorderThickness="0,0,0,3"
                            VerticalAlignment="Bottom"
                            Visibility="Visible"/>

                        <Border x:Name="LineTop"
                            BorderBrush="{DynamicResource AccentColor}"
                            BorderThickness="0,3,0,0"
                            VerticalAlignment="Top"
                            Visibility="Collapsed"/>

                        <ItemsPresenter x:Name="ItemsHost"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                    HorizontalAlignment="Left"
                                    Margin="15,0,15,0"
                                    TextBlock.FontSize="15"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=(TransparentAndBottomLineListBox:ListBoxHelper.LinePosition), RelativeSource={RelativeSource Self}}" Value="Top">
                            <Setter TargetName="LineBottom" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="LineTop" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ItemsHost" Property="VerticalAlignment" Value="Top"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=(TransparentAndBottomLineListBox:ListBoxHelper.LinePosition), RelativeSource={RelativeSource Self}}" Value="Bottom">
                            <Setter TargetName="ItemsHost" Property="VerticalAlignment" Value="Bottom"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <sys:Double x:Key="ScalePressed">0.93</sys:Double>
    <sys:Double x:Key="ScaleNormal">1.0</sys:Double>
    <sys:Double x:Key="OpacityPressed">0.75</sys:Double>
    <sys:Double x:Key="OpacityNormal">1.0</sys:Double>
    <Duration x:Key="TimePressDuration">0:0:0.06</Duration>
    <Duration x:Key="TimeReboundDuration">0:0:0.35</Duration>


    <Style x:Key="HorizontalButtonGroupStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="AlternationCount" Value="1000"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border x:Name="MainBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3"
                        Padding="1"
                        SnapsToDevicePixels="True">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                    HorizontalAlignment="Stretch"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColorSecondary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="Tag" Value="Uniform">
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1" IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="GroupButtonItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Padding" Value="4,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid x:Name="RootGrid">
                        <Grid x:Name="AnimationContainer" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform x:Name="AnimatedScale" ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>

                            <Border x:Name="HoverBorder"
                                Background="{DynamicResource AccentColorSecondary}"
                                BorderThickness="0"
                                CornerRadius="2"
                                Margin="1,0"
                                Opacity="0"/>

                            <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="2"
                                Margin="1,0">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>

                        <Rectangle x:Name="SeparatorLine"
                               Width="1"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Stretch"
                               Margin="0,4" 
                               Stroke="{DynamicResource Border}"
                               StrokeThickness="1"
                               StrokeDashArray="4 2" 
                               SnapsToDevicePixels="True"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource TimeReboundDuration}"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <EventTrigger RoutedEvent="UIElement.PreviewMouseLeftButtonDown">
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="AnimatedScale" Storyboard.TargetProperty="ScaleX" To="{StaticResource ScalePressed}" Duration="{StaticResource TimePressDuration}"/>
                                    <DoubleAnimation Storyboard.TargetName="AnimatedScale" Storyboard.TargetProperty="ScaleY" To="{StaticResource ScalePressed}" Duration="{StaticResource TimePressDuration}"/>
                                    <DoubleAnimation Storyboard.TargetName="AnimationContainer" Storyboard.TargetProperty="Opacity" To="{StaticResource OpacityPressed}" Duration="{StaticResource TimePressDuration}"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <EventTrigger RoutedEvent="UIElement.PreviewMouseLeftButtonUp">
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="AnimatedScale" Storyboard.TargetProperty="ScaleX">
                                        <LinearDoubleKeyFrame Value="{StaticResource ScalePressed}" KeyTime="0:0:0.06"/>
                                        <EasingDoubleKeyFrame Value="{StaticResource ScaleNormal}" KeyTime="0:0:0.41">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <ElasticEase Oscillations="1" Springiness="4" EasingMode="EaseOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                    </DoubleAnimationUsingKeyFrames>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="AnimatedScale" Storyboard.TargetProperty="ScaleY">
                                        <LinearDoubleKeyFrame Value="{StaticResource ScalePressed}" KeyTime="0:0:0.06"/>
                                        <EasingDoubleKeyFrame Value="{StaticResource ScaleNormal}" KeyTime="0:0:0.41">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <ElasticEase Oscillations="1" Springiness="4" EasingMode="EaseOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                    </DoubleAnimationUsingKeyFrames>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="AnimationContainer" Storyboard.TargetProperty="Opacity">
                                        <LinearDoubleKeyFrame Value="{StaticResource OpacityPressed}" KeyTime="0:0:0.06"/>
                                        <LinearDoubleKeyFrame Value="{StaticResource OpacityNormal}" KeyTime="0:0:0.41"/>
                                    </DoubleAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter TargetName="SeparatorLine" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GroupInputItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid x:Name="RootGrid">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="2" Margin="1,0">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <Rectangle x:Name="SeparatorLine"
                               Width="1"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Stretch"
                               Margin="0,4" 
                               Stroke="{DynamicResource Border}"
                               StrokeThickness="1"
                               StrokeDashArray="4 2" 
                               SnapsToDevicePixels="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter TargetName="SeparatorLine" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GroupTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource FontLight}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <TextBlock Style="{StaticResource TextBlockLight}"
                                x:Name="PlaceholderText"
                                Text="{TemplateBinding Tag}"
                                Foreground="#888888" 
                                Opacity="0.6"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="2,0,0,0"
                                IsHitTestVisible="False"
                                Visibility="Collapsed"/>

                        <ScrollViewer x:Name="PART_ContentHost" 
                                  Focusable="false" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value=""/>
                                <Condition Property="IsFocused" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>