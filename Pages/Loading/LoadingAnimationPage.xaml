<Page x:Class="PlayVoice.Pages.Loading.LoadingAnimationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PlayVoice.Pages.Loading"
      mc:Ignorable="d"
      >

    <Border>
        <Grid>

            <Grid Grid.Row="0">
                <Path 
                      Stroke="{DynamicResource AccentColor}" StrokeThickness="8"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="M 50,0 A 50,50 0 1 1 0,50"
                      RenderTransformOrigin="0.5,0.5"
                      HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Path.RenderTransform>
                        <RotateTransform x:Name="rotateTransform" Angle="0"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>

            <!--<TextBlock Grid.Row="0"
                        Margin="0,,0,0"
                        FontSize="13"
                        Style="{StaticResource TextBlockLight}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Text="{Binding Text}"/>-->
        </Grid>
    </Border>
    <Page.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation 
                        Storyboard.TargetName="rotateTransform"
                        Storyboard.TargetProperty="Angle"
                        From="0" To="360" Duration="0:0:1"
                        RepeatBehavior="Forever"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
</Page>
