<UserControl x:Class="PlayVoice.Pages.Workshop.DetailPageItem"
             x:Name="self"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlayVoice.Pages.Workshop"
             mc:Ignorable="d">
    <Border Grid.Row="0"
            BorderBrush="{DynamicResource Border}"
            BorderThickness="0,1,0,0"
            Margin="2,3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    ClipToBounds="True" 
                    Background="Transparent" 
                    x:Name="ScrollingTextBorder"
                    Margin="3,2,3,2.5">
                <TextBlock x:Name="ScrollingText" 
                           Text="{Binding ItemTitle}"
                           Style="{StaticResource TextBlockLight}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           FontSize="11">
                </TextBlock>
            </Border>
            <Border Grid.Row="1" Opacity="0.5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" 
                           Text="{Binding ItemDuration}"
                           Style="{StaticResource TextBlockMedium}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="11"
                           Margin="5,0"/>
                    <TextBlock Grid.Column="1" 
                           Text="{Binding ItemSize}" 
                           Style="{StaticResource TextBlockMedium}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="11"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
